/** @type {import('next').NextConfig} */
const nextConfig = {
  // O selo de dev fica fixo no canto inferior esquerdo e apareceria em todo
  // print do shot.mjs, poluindo a comparação visual.
  devIndicators: false,
}

export default nextConfig
